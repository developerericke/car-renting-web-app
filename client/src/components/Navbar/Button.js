import React from 'react'
import './Button.css'

const Button = () => {
    return (
        <a href="/login">
            <button className="btn">Login | SignUp</button>
        </a>
    )
}

export default Button
